'use strict';

const chai = require('chai');
const expect = chai.expect;
const annotatedArray = require('../../explore/annotatedArray')();

describe('Array creation', function() {

  it('does stuff', function() {

    let array = annotatedArray.create();
    array.setAnnotator(    })

    array.length = 300;
    array.fill(1);
    let annotated = array.get(40);
    debugger;

  });

});



